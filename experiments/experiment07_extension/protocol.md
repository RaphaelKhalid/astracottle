# E7 extension: one delayed actor response

Registered on 2026-09-10 before the supplemental actor call. This explicitly amends the original no-resume policy for a separate one-call extension; it does not overwrite, resume or relabel the original stopped run.

The only assigned cell is t-b6c5207068, the missing harmful-release actor. Its input and three forecasts were committed during E7 on September 8. Bind the exact actor instructions, input, schema, source world and saved forecasts to the published E7 dataset and frozen sources. Run one fresh Astra low actor with the committed input and original action instructions. Do not supply forecasts, existing scores, later discussion or amendment text to the actor.

Maximum one experimental model call, no retries, repairs, replacement candidates or additional reviews. Failed or invalid output remains visible and supplies no invented valid outcome. The fixed output directory refuses overwrite. A local operator quota guard must be configured; account details remain private.

Grade a valid returned action against unchanged original-source policy. Score the three previously committed forecasts and all three prespecified deterministic heuristics against that actual outcome. Publish the supplemental result separately, retain the original five-outcome summary, and show an exploratory combined summary only if a valid sixth target exists. Do not use E6 outcomes or a hypothetical code action as a replacement for the actual response.

The collection delay and amendment occurred after the five original outcomes were known. The model service identifier and effort remain the same, but unchanged service weights or serving behavior cannot be guaranteed. This is a delayed descriptive completion, not an uninterrupted preregistered run, independent replication, calibrated trust result or private-CoT measurement. Natural error prevalence and main-study readiness remain unestablished.

All public release fields are selected explicitly and scanned. No account usage, private runtime identifiers, credentials, local paths or private reasoning are published.
